import { NavigationContainer } from '@react-navigation/native';

import { TabNavigation } from './Tab';
export const Routes = () => {
  return (
    <NavigationContainer>
      <TabNavigation />
    </NavigationContainer>
  );
};
