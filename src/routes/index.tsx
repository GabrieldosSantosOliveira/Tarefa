import { NavigationContainer } from '@react-navigation/native';

import { Stack } from './Tab';
export const Routes = () => {
  return (
    <NavigationContainer>
      <Stack />
    </NavigationContainer>
  );
};
